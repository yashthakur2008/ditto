steps:
  - name: "gcr.io/cloud-builders/docker"
    args:
      - build
      - "-t"
      - "gcr.io/$PROJECT_ID/$REPO_NAME:$COMMIT_SHA"
      - "."

  - name: "gcr.io/cloud-builders/docker"
    args:
      - push
      - "gcr.io/$PROJECT_ID/$REPO_NAME:$COMMIT_SHA"

  - name: "gcr.io/google.com/cloudsdktool/cloud-sdk"
    entrypoint: gcloud
    args:
      - run
      - deploy
      - "$REPO_NAME"
      - "--image=gcr.io/$PROJECT_ID/$REPO_NAME:$COMMIT_SHA"
      - "--region=us-central1"
      - "--platform=managed"
      - "--allow-unauthenticated"
      - "--set-env-vars=FIREBASE_PROJECT_ID=$PROJECT_ID,APP_ENV=production,OPENAI_MODEL=gpt-4o-mini,CLAUDE_MODEL=claude-3-5-sonnet-latest,CORS_ORIGINS=http://localhost:3000"

images:
  - "gcr.io/$PROJECT_ID/$REPO_NAME:$COMMIT_SHA"
